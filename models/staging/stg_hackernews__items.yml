version: 2

models:
  - name: stg_hackernews__items
    description: Staged items data from the Hacker News API. One row per item.
    columns:
      - name: item_id
        description: The unique identifier for the item.
        tests:
          - unique
          - not_null